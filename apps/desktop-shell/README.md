# apps/slm-desktop

Main desktop shell application target.

Owns:
- shell UI (`Qml/`)
- shell services/managers (network, bluetooth, sound, notification, global menu)
- runtime orchestration (`main.cpp`, `DesktopScene`)

Current implementation still resides at repository root; this folder defines the target location.
