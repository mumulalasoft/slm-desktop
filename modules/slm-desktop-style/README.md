# slm-desktop-style

Reusable Qt Quick Controls style package for Slm Desktop apps.

Planned contents:
- `Style/qmldir`
- all QML style controls (`Menu`, `MenuItem`, `Switch`, `Button`, ...)
- shared visual tokens for app-level consistency

Current source of truth is still root `Style/`. This module is a migration target.
