# Placeholder for standalone test migration.
# Tests remain in monorepo under `tests/filemanager/` during transition.

message(STATUS "slm-filemanager tests are not migrated yet; using monorepo test suite.")
