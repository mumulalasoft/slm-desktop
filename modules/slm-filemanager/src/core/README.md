# Core boundary

This directory marks the future `slm-filemanager` core boundary.

Planned contents after extraction:
- public headers (`FileManagerApi`, `FileManagerModel`, factories),
- domain modules (`recent`, `openwith`, `storage`, `ops`),
- internal helpers with stable API surface.
