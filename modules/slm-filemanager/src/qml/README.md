# QML boundary

This directory marks the future standalone QML package root.

Planned mapping:
- source: `Qml/apps/filemanager/*`
- package import root: `SlmFileManager` (or equivalent)
- compatibility shim in shell for one release cycle after split.
