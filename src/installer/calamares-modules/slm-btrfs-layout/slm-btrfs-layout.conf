---
# Config for slm-btrfs-layout Calamares job module.
# See src/installer/calamares-modules/slm-btrfs-layout/SlmBtrfsLayoutJob.cpp.

# When true, the module logs the scratch-mount + subvolume create +
# staging-mount sequence it would run and returns ok. When false, the
# module performs the operations on the partition identified by
# slm.target.root_device — guarded additionally by
# slm.target.confirmed=true.
dry-run: true
